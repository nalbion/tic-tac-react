import React from 'react';

export const Privacy = ({className}) => {
    return (
        <div className={className}>
            This application may details required to connect to your devices.
            We do not share the information with anyone.
        </div>
    );
};
