import React from 'react';

export const GooglePermissions = ({className}) => {
    return (
        <div className={className}>
            We need to give Google permssion to create an account and integrate with our
            Google Assistant app.
        </div>
    );
};
